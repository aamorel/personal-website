type Work = {
  title: string;
  description: string;
  url: string;
};

type Company = {
  title: string;
  description: string;
  url: string;
};

type Research = {
  title: string;
  url: string;
};

type Problem = {
  text: string;
  problem: string;
  hint: string;
};
